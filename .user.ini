; Read by PHP when running as CGI or FPM. Ignored by mod_php and by the
; built-in server (php -S) - for those see .htaccess and the README.
upload_max_filesize = 512M
post_max_size = 512M
memory_limit = 512M
max_execution_time = 300
max_input_time = 300
